# Local configuration (contains secrets)
config.json
!config.example.json

# Instance encryption key for secrets at rest (PLANNING/overhaul-2/02).
# Generated on first boot when ORCHESTRA_SECRET_KEY isn't set. Losing it means
# re-entering any stored GitHub token / API key.
secret.key

# SQLite database (task state)
orchestra.db
*.db-journal
*.db-wal
*.db-shm

# Node / TypeScript
node_modules/
server/dist/
server/public/
client/dist/
*.tsbuildinfo

# macOS
**/.DS_Store
**/.AppleDouble
**/.LSOverride

# Windows
**/Thumbs.db
**/ehthumbs.db
**/Desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs / temp
**/*.log
**/*.tmp
**/*.bak
**/*.cache

# Planning (internal)
/PLANNING/

# VitePress (build artifacts)
docs/.vitepress/dist/
docs/.vitepress/cache/

# Test coverage (build artifacts)
server/coverage/
